### Jazz Ignore 0
# 属性 core.ignore 指定将在此目录中忽略的文件模式的列表。
#
# core.ignore.recursive 的值将被忽略。
#
# 将不会落实已忽略的文件和文件夹，
# 但是在接受或更新期间可以修改这些文件和文件夹。
# 忽略属性应该包含以空格分隔的文件名模式列表。
# 每个模式都区分大小写并括在花括号“{”和“}”中。
# “*”与零个或多个字符匹配，“?”与单个字符匹配。
#
#   例如：{*.sh} {\.*} 忽略 shell 脚本和隐藏文件

# 注意：修改忽略文件并不会更改派生的资源的忽略状态。

core.ignore.recursive= 

core.ignore= \
	{Thumbs.db} 