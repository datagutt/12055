<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >

    <LinearLayout android:id="@+id/notitle_dialog_body"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:layout_gravity="center_vertical"
        android:minWidth="300dip"
        android:paddingTop="11dip"
        android:paddingLeft="20dip"
        android:paddingRight="20dip"
        android:paddingBottom="5dip">

        <ProgressBar android:id="@+id/notitle_dialog_progressbar"
    	    style="@android:style/Widget.OPPO.ProgressBar.Inverse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:max="10000"
	        android:paddingRight="4dip"  

            android:layout_marginRight="12dip" />

        <TextView android:id="@+id/notitle_dialog_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/oppo_alert_dialog_content_text_color"

            android:textSize="@android:dimen/oppo_dialog_content_text_size"
            />
    </LinearLayout>
</FrameLayout>
